# Userspace — reservado

Não existe userspace executável na versão 0.0.1. O primeiro processo futuro
se chamará `init`; o prompt do shell será `utamo>`. Binários ELF ring 3 dependem
de processos, isolamento de memória, loader e syscalls reais. Nada desta pasta
é compilado pelo build atual do kernel.
