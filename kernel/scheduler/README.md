# Scheduler — planejado

Threads e context switching virão após exceções, memória e heap. Separar thread
de processo e reservar espaço conceitual para dados por CPU, idle e estados de
bloqueio. Não existem threads, temporizador, preempção ou SMP nesta versão.
